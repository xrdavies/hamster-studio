# Skill 第一阶段试用

Skill 是使用现有工具的创作流程。它不会新增模型能力，也不能执行脚本或外部程序。

## 使用内置流程

也可以从左侧导航栏底部的 **Skills** 打开管理面板。管理面板用于查看内置和本地 Skill、创建副本和删除本地 Skill；聊天输入框的 Skills 按钮只负责当前会话选择。

1. 选择支持工具调用的聊天模型；图片工作流还需在图片设置中选择图片模型，看图需要视觉能力。
2. 点击输入框的 **Skills**，选择 **Character Sheet · 角色设定**。
3. 添加参考图或描述角色，发送消息。默认交付一张包含正、侧、背视图的角色设定图；不是骨骼绑定素材。
4. 选择 **普通对话** 可停用 Skill。选择仅影响当前会话。

角色设定 v2 默认采用自然站姿三视图，统一角色特征、比例、基线、画风与配件位置；存在左右不对称特征时建议增加另一侧。左右以角色自身为准，未知背面细节会说明为设计补全。生成后查看结果并报告方向、角色一致性、肢体和裁切等问题；额外付费修改前确认。表情、换装和特写仅在用户要求时扩展。

已有会话保留旧快照。使用更新后的内置提示词，需要重新打开 Skills 并选择角色设定。

## 用 Creator 制作 Skill

1. 点击 Skills 右上角 **创建 Skill**，返回对话后描述输入、期望产物和制作要求。
2. Creator 可询问缺少的信息，通过 `draft_skill` 生成草稿卡片；不会自动保存或执行图片生成。
3. 点击卡片，检查或修改名称、说明、工具声明和流程指令。
4. 点击 **选择并试运行**，在当前会话补充素材并发送消息。此操作是真实任务，图片生成可能计费，不会自动保存 Skill。
5. 可从原草稿卡片返回编辑；要让 Creator 修改，再次点击创建 Skill 切回 Creator 并说明试用反馈。运行结果不会自动被标记为“通过”。
6. 满意后在草稿面板点击 **确认并保存副本**。然后从 Skills 列表选择保存的版本使用。

## 存储与版本

内置文件在 `src-tauri/skills/`，构建时嵌入程序。本地 Skill 存放在应用数据目录的 `skills/`，一个 JSON 文件包括：

```json
{
  "id": "由应用生成",
  "version": 1,
  "name": "流程名称",
  "description": "用途和交付物",
  "tools": ["create_images", "list_images", "view_image"],
  "instructions": "输入要求、制作步骤、输出规格、验收标准和限制"
}
```

第一阶段不覆盖旧版本：编辑已有 Skill 会生成副本，保存分配新 ID。会话保存完整 Skill 快照，任务和中断后的继续使用任务开始时的快照；升级内置 Skill 后需重新选择才应用新内容。

只向 Agent 注册声明的工具。Creator 专用 `draft_skill` 不开放给本地 Skill。工具归属检查、审批和任务预算仍由应用执行。普通模式的手动操作（如图片重试、导出）沿用原有权限。

## 范围与限制

- 普通 Skill 支持 `create_images`、`list_images`、`view_image`、`read_webpage`；未声明的工具不可调用。
- 每任务最多生成 3 张图片、6 轮模型调用。Skill 不能提高预算。
- 序列帧编码、精灵图拼接、骨骼绑定、外部动画工具尚未提供；Creator 应说明缺失能力。
- 本阶段不提供远端市场、自动更新、脚本执行和导入导出界面；本地 JSON 可手动维护。
- 校验通过只表示结构与工具声明合法，不表示提示词或产物质量已验证。应使用实际模型进行试用。

## 六段式模板与预检

新草稿使用 `schemaVersion: 2`（不同于内容 `version`）。标题固定为英文，正文可用中文；按顺序填写六段，每段不能为空：

```markdown
## Purpose

用途：只聚焦一个成果。

## Inputs

必需输入、可选输入及默认值。

## Outputs

交付物及不包含的内容。

## Steps

依次使用什么工具、什么时候结束。

## Acceptance

可以观察的结果检查标准。

## Limits

缺少信息、不支持的能力及额外付费修改如何处理。
```

声明输入与能力要求：

```json
{ "minImages": 0, "maxImages": 6, "capabilities": ["vision"] }
```

上述对象放在 `requirements` 字段。常用能力为 `vision`、`image_edit`、`multi_reference`、`mask`，可声明尚未支持的工具与能力，但不会自动获得其执行权限。参考图数量范围应为 0–6。

发送前显示缺失输入、缺少图片模型、缺少选区以及未知工具和能力提示。用户可取消调整，或确认继续尝试；格式错误仍会阻止保存和运行。当前模型表只区分聊天/图片，工具调用、视觉和编辑能力均标为未验证，不宣称已通过。没有可用对话 Provider 或 API Key 时仍无法实际发送请求。

旧 schemaVersion 1 工作流继续兼容原指令格式；新 Creator 输出必须符合六段式规范。已有会话重新选择新版 Creator 才会获得更新的模板要求。界面可修改参考图范围和能力声明；试运行不会自动保存。
