# Skill 第一阶段试用

Skill 是使用现有工具的创作流程。它不会新增模型能力，也不能执行脚本或外部程序。

## 使用内置流程

1. 选择支持工具调用的聊天模型；图片工作流还需在图片设置中选择图片模型，看图需要视觉能力。
2. 点击输入框的 **Skills**，选择 **Character Sheet · 角色设定**。
3. 添加参考图或描述角色，发送消息。默认交付一张包含正、侧、背视图的角色设定图；不是骨骼绑定素材。
4. 选择 **普通对话** 可停用 Skill。选择仅影响当前会话。

## 用 Creator 制作 Skill

1. 在 Skills 中选择 **Skill Creator**，描述输入、期望产物和制作要求。
2. Creator 可询问缺少的信息，通过 `draft_skill` 生成草稿卡片；不会自动保存或执行图片生成。
3. 点击卡片，检查或修改名称、说明、工具声明和流程指令。
4. 点击 **选择并试运行**，在当前会话补充素材并发送消息。此操作是真实任务，图片生成可能计费，不会自动保存 Skill。
5. 可从原草稿卡片返回编辑；要让 Creator 修改，重新选择 Creator 并说明试用反馈。运行结果不会自动被标记为“通过”。
6. 满意后在草稿面板点击 **确认并保存副本**。然后从 Skills 列表选择保存的版本使用。

## 存储与版本

内置文件在 `src-tauri/skills/`，构建时嵌入程序。本地 Skill 存放在应用数据目录的 `skills/`，一个 JSON 文件包括：

```json
{
  "id": "由应用生成",
  "version": 1,
  "name": "流程名称",
  "description": "用途和交付物",
  "tools": ["create_images", "list_images", "view_image"],
  "instructions": "输入要求、制作步骤、输出规格、验收标准和限制"
}
```

第一阶段不覆盖旧版本：编辑已有 Skill 会生成副本，保存分配新 ID。会话保存完整 Skill 快照，任务和中断后的继续使用任务开始时的快照；升级内置 Skill 后需重新选择才应用新内容。

只向 Agent 注册声明的工具。Creator 专用 `draft_skill` 不开放给本地 Skill。工具归属检查、审批和任务预算仍由应用执行。普通模式的手动操作（如图片重试、导出）沿用原有权限。

## 范围与限制

- 普通 Skill 支持 `create_images`、`list_images`、`view_image`、`read_webpage`；未声明的工具不可调用。
- 每任务最多生成 3 张图片、6 轮模型调用。Skill 不能提高预算。
- 序列帧编码、精灵图拼接、骨骼绑定、外部动画工具尚未提供；Creator 应说明缺失能力。
- 本阶段不提供远端市场、自动更新、脚本执行和导入导出界面；本地 JSON 可手动维护。
- 校验通过只表示结构与工具声明合法，不表示提示词或产物质量已验证。应使用实际模型进行试用。
