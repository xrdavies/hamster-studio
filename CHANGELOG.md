# 变更记录

本文件以中文维护。README 仅保留项目介绍、使用说明和开发入口；后续开发变更在此记录。

## 未发布

- 全局 Skills 管理改为完整页面，左侧区分内置与用户流程，右侧查看和编辑长文本，新增模板创建、Creator 新会话、草稿试运行及未保存提醒。
- 会话 Skill 弹窗仅提供选择，不再包含管理操作；删除用户 Skill 保留会话快照，路径限制为本地 UUID 标识。

- 左侧底部新增 Skills 管理入口，支持查看内置与本地 Skill、创建、复制编辑和删除本地 Skill；聊天输入框仍用于当前会话选择。

- Skill 界面拆分为流程选择与草稿编辑，新增明确的创建入口、当前选择标记与中文内置说明；创建后引导回到输入框，返回列表保留草稿。

- Skill 新草稿采用六段式模板与 schemaVersion 2，Creator 按模板生成，旧格式继续兼容。
- 增加参考图和能力要求编辑及发送前预检：缺失或未验证的能力仅提示，用户可确认继续；不会因此授予不存在的工具。

- 角色设定 Skill 升至 v2：完善自然站姿三视图、左右不对称、固定特征、统一排版及生成后检查规范，保持已有会话快照不变。

- 新增第一阶段本地 Skill 机制：会话选择、任务快照、工具声明校验和受限工具注册。
- 内置 Skill Creator 与角色设定示例；支持草稿审阅编辑、手动试运行、确认保存副本，详情见 `SKILLS.md`。

- 批量结果显示已提交步骤的完成数量、结果编号与原图查看，支持一次导出全部已生成结果到独立文件夹。
- 失败步骤可显式补做缺失结果，沿用原提示词、参考图和遮罩，使用当前图片模型，保留成功结果与原失败记录；每任务 3 张的限制保持不变。

- 局部修改支持橡皮擦、撤销上一步、缩放查看、重新打开已保存的选区，并可在历史消息中查看修改区域。

- 调整桌面标题栏：macOS 原生窗口按钮右侧展示 Logo 与 Hamster Studio；移除侧栏重复品牌区，保留内容区会话标题，增加侧栏收起与展开按钮。

- 明确图片工具的融合、变体、逐张处理模式。逐张处理必须显式指定一张原图、生成一张结果；错误批处理参数在调用图片接口前返回可纠正错误，避免重复提交全部参考图。
- 系统提示词要求逐张修复使用独立提示词，并禁止仅凭提示词承诺输出尺寸、透明度或无损还原。

- 修复局部修改选区状态被固定宽度按钮挤成竖排的问题；参考图附件统一为紧凑卡片，多图自动换行，选区状态使用强调色。

- 修复本地图片选择器只能单选的问题，支持一次选择多张；超过剩余参考图名额时提示错误，取消选择保留已有附件。

### 局部修改

- 参考图附件增加局部修改入口：涂抹选区、调节画笔大小、清空并重新选择。
- 选区转为与原图同尺寸的 PNG 遮罩，透明区域交给模型修改；同时支持 Agent 和直接图片编辑。
- 遮罩绑定原图、随会话请求保存，保留原图。模型需支持遮罩编辑；不保证区域外逐像素不变。

### 多参考图

- 支持累积添加、移除和一次拖入多张参考图，最多 6 张，每张仍限制 10 MB。
- 多图传入 Agent 视觉上下文与图片编辑接口，聊天历史展示全部参考图，兼容旧单图记录。
- 图片接口使用单图 `image`、多图 `image[]`；模型需要支持相应编辑能力。

### 文档与提示词

- 清理中英文 README 末尾累积的开发说明，集中整理本文件。
- 系统提示词迁至 `src-tauri/prompts/image-agent.txt`，使用 Rust `include_str!` 在构建时嵌入程序；修改源码提示词需要重新构建。
- 会话参考图 ID 与用户会话指令仍由运行时追加。工具权限、次数预算和图片归属校验由代码执行，不依赖提示词作为安全边界。

## 近期开发整理

以下内容汇总此前开发变更，不对应某一个已发布版本。

### 图片 Agent 工作流

对话模型可以查询当前会话图片（`list_images`）、查看图片（`view_image`），以及生成或指定原图编辑（`create_images` 的 `source_image_id` 参数）。例如：“把第二张图的背景改成蓝色，再检查一下结果。”每次编辑都会保留原图并生成新版本。

查看图片需要对话模型同时支持工具调用和视觉输入。用户选中的参考图、Agent 请求查看的图片，会作为多模态用户消息发送给对话模型的 Provider。图片保存在本地，历史记录仅保存引用，不保存 Base64；旧图可通过 `view_image` 再次加载。生成成功不代表 Agent 已经看过图片。

每次任务最多生成 3 张、执行 6 轮模型调用、查看 6 张图片（每张不超过 20 MB）。多张或追加生成需要确认。不支持视觉输入的模型可能返回错误，此时需切换兼容模型后重试。

可从输入框导入本地 PNG、JPEG、WebP 图片（不超过 10 MB）。应用保存当前会话专属的本地副本，可用于看图、Agent 编辑或直接调用图片模型编辑。移除附件只清除输入框的选择。

界面翻译使用 `src/shared/locales/` 中的类型化语义键名，动态内容使用 `{name}` 占位符。界面文案通过 `t(key, parameters)` 获取，应用错误代码或外部消息通过 `translateMessage` 展示，不反向翻译用户内容或 Provider 错误。

### Agent 中断与重试

图片步骤在发送前持久化请求标识和发送状态，每张完成后保存结果。同一任务重复调用已完成的相同步骤会复用结果；当前会话存在相同请求或结果未知的步骤时，后续图片请求需确认。重启不会自动重放付费工具，已生成的图片仍保留。该保护不能替代 Provider 侧幂等：取消、断网或超时后服务端可能继续处理，请检查服务商记录后再生成。

连接超时 30 秒，对话模型单次 HTTP 请求上限 10 分钟，单张图片完整请求上限 10 分钟，等待用户确认上限 15 分钟，整个任务上限 30 分钟。图片超时、模型错误和用户取消分别展示；错误详情可展开。部分图片成功时保留结果，不自动重试剩余请求。

在聊天中提供公开 HTTP(S) 链接并要求读取或总结即可，无需搜索 API Key。仅允许读取当前会话用户提供的链接。支持 HTML 和纯文本，超时 30 秒，响应最多 2 MB，正文截取最多 16000 字符。不支持 JavaScript、登录和 PDF，不访问内网，也不走系统代理；提取内容会发送给对话模型服务商。

当域名仅解析到代理 Fake-IP 网段（198.18.0.0/15）时，网页读取器通过 Cloudflare 加密 DNS 查询真实公网 IPv4 地址，校验后固定连接地址。仅域名发送给解析服务；内网和直接输入的保留 IP 仍被拦截。

当前轮尚未输出内容时，对话模型临时错误最多自动重试两次（约 2 秒、5 秒并加入随机延迟）。检查点保留待执行模型消息和已完成工具结果。部分回复中断后可点击“继续此步骤”；图片失败和用户取消不重放。网页读取在原 30 秒预算内重试。Rig 当前错误接口未暴露 Retry-After 响应头，模型列表获取在 30 秒预算内最多重试两次，并遵循数值形式的 Retry-After。
